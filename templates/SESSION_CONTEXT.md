# Session Context

> Generated by `scripts/amnesia/session-start.sh` — this template documents the
> structure. The recorder captures **operational project context only**: git
> state, task, and notes. It never captures model reasoning or chat history.

- **Task:** <short task description>
- **Started:** <YYYYMMDDTHHMMSSZ> (UTC)
- **Repo path:** <absolute path on the local machine — runtime only, gitignored>
- **Branch at start:** <branch>
- **HEAD at start:** <short sha>

## Git state at start

```
<git status -sb output>
```

### Diff stat
```
<git diff --stat output>
```

### Changed files
```
<git diff --name-only output>
```

## Notes

(free-form notes; `session-save.sh --note` also appends to the session log)

## Risks

(known risks for the resuming session — edit by hand)

## Next actions

(planned next steps, first incomplete one is the resume point — edit by hand)

## Session log

- <timestamp> — SESSION START — task: <task>
- <timestamp> — SAVE — <note> (branch: <branch>, HEAD: <sha>)
- <timestamp> — SESSION CLOSE — <summary>
